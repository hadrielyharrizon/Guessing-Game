# Guessing-Game
Simple guessing game in Python. The program defines a secret number and the user tries to guess it by entering a value. It uses input(), variables, and conditional statements (if/else) to check the guess and display success or error messages.
